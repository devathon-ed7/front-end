import { Box } from '@mui/material';
import { Breadcrumb } from '../components/Breadcrumb';

export const PermissionPage = () => {
  return (
    <Box>
      <Breadcrumb />
    </Box>
  );
};